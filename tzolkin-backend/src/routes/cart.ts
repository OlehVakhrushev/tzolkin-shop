import express from "express";

const router = express.Router();

// Временная заглушка — позже добавим логику
router.get("/", (req, res) => {
  res.send("Cart route - to be implemented");
});

export default router;
