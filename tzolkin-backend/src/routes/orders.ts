import express from "express";

const router = express.Router();

// Временная заглушка — позже добавим логику
router.get("/", (req, res) => {
  res.send("Orders route - to be implemented");
});

export default router;
